`src` contains the source code for the Ruby server.

`templates` contains the HTML templates for the password recovery servlet.

`table_setup.sql` describes the MySQL table setup that the server relies on.